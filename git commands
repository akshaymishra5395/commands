git reset HEAD -- <file> |<directory>                      remove file and directory from sttaging 
git checkout -f                                            remove modified chaneges for all files

git log -p -1             ----diff in files of previous one commit 
git diff                  ---diff with modified and staged file
git diff --staged         ---diff with modified and last commited code


 
git rm waste.html                     ----removes file from staged and working directory on commit
git rm --cashed waste.html            ----removes file  only from staged area and left untracked in working area
git commit -a -m "removing waste"    
